#Updating server port from default 8080 to something different
server.port = 8080

#This to identify them in complex projects as well as to link spring cloud config file to project.
spring.application.name=limits-service
#spring-cloud-config-server URL and it will pickup default
spring.cloud.config.uri=http://localhost:8888
#profile for spring cloud config
spring.profiles.active=plab

